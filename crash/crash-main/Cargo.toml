[package]
name = "crash_main"
version = "0.0.0"
edition = "2021"

[dependencies]
crash_ir_parser = { path = "../../cir/cir-parser" }
clap = { version = "4.4.12", features = ["derive"]}
tokio = { version = "1.0.0", features = ["rt", "rt-multi-thread", "macros"] }