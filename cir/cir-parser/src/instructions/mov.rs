use crash_ast::instructions::MoveInstruction;
use crate::parser::Parser;

impl Parser {
    
    pub(super) fn try_parse_mov_instruction(&self) -> Option<MoveInstruction> {
        
        
        None
    }
}